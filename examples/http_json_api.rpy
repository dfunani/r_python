# struct CreateUser:
#     name: str
#     email: str
#
# @app.post("/users")
# def create_user(body: CreateUser) -> int:
#     print(body.name)
#     return 201

def main() -> int:
    print("JSON handlers — serde + net modules on roadmap")
    return 0
