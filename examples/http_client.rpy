# Roadmap (P10+): stdlib/net/http.rpy
# import net.http

def main() -> int:
    # resp = http.get("https://api.example.com/v1/status")
    # if resp.ok:
    #     print(resp.body)
    print("HTTP client — planned stdlib/net/http")
    return 0
