# Roadmap: stdlib/net/http_server.rpy
# import net.http_server as srv

def main() -> int:
    # app = srv.App()
    # @app.get("/health")
    # def health() -> str:
    #     return "ok"
    # app.listen("127.0.0.1:8080")
    print("HTTP server — planned for v2.x stdlib")
    return 0
